- [ ] BUG-1: Got error: "Failed to download file from OneDrive" when creating a new data file in OneDrive
- [x] BUG-2: Add a new dashboard button next to Money Tree label in the navigation to improve UX
- [x] BUG-3: "Failed to Load File: File permission expired. Please click the Sync button to grant permission again." for auto sync when loading from local storage
- [x] BUG-4: Use USD as intermediate rate if the direct rate isn't found from API
- [x] BUG-5: In balance sheet report, we should exchange rate at the valuation time for currency conversion
- [x] BUG-6: Some rates failed to fetch. Check console for details
- [x] BUG-7: Ensure rates are loaded before showing the report
- [x] BUG-8: No currency conversion in the dashboard (i,e: networth)
- [ ] BUG-9: "Invalid time value" when editing time field
- [x] BUG-10: Expense transactions don't show in cash flow report - ROOT CAUSE: Transaction type linked to non-existent category. Cash flow report silently skips transactions where category lookup fails
- [ ] BUG-11: App allows orphaned transaction types (linked to non-existent categories) to be selected, creating invalid transactions that don't appear in reports. Should validate category existence or filter out orphaned types from dropdowns
- [x] BUG-12: Fix archiving files for shared folder
- [x] BUG-13: Backup prompt doesn't show when file is loaded for the first time and has never been backed up (lastBackupDate is null but baseVersion exists)
- [x] BUG-14: Dashboard cashflow calculation doesn't handle multi-currency properly - sums transaction amounts without converting to base currency
- [x] BUG-15: Transaction schema missing fromAssetId and toAssetId fields - caused asset-linked transactions to lose their asset references when saved and reloaded from file
- [x] BUG-16: Income Details & Expense Details empty in cashflow reports - ROOT CAUSE: useEffect calculating chart data didn't include cashFlow in dependencies, so detail tables didn't update when cashFlow data changed
- [x] BUG-17: unsavedChanges flag incorrectly set to false after syncing when changes are made during the sync operation - FIX: Capture snapshot timestamp when gathering data, clear flag before save. If changes occur during save, store actions automatically set flag back to true. lastModified/lastSaved use snapshot time. If save fails, flag remains unchanged.
- [x] BUG-18: Dashboard, recent transactions, and budgets page always show $ icons for other currencies - ROOT CAUSE: FinancialSummary.tsx and BudgetProgressBar.tsx had hardcoded 'USD' in formatAmount functions; RecentTransactionsList.tsx used baseCurrency instead of transaction's account currency - FIXED: Removed local formatAmount functions and use formatCurrency utility. Dashboard cards use baseCurrency (for aggregated values), recent transactions now show each transaction in its account's currency.
- [x] BUG-19: OneDrive no longer shows shared folders - ROOT CAUSE: OneDriveProvider.listDriveItems() only called '/me/drive/root/children' which doesn't include shared folders. FIXED: Now fetches both '/me/drive/root/children' and '/me/drive/sharedWithMe' in parallel and merges the results when listing root items.
- [ ] BUG-20: Race condition when StorageService.initialize() returns false but has cached file - User loses their cached file reference just by dismissing the reconnect dialog once. ROOT CAUSE: When provider.initialize() returns false but provider has cached file name, code shows reconnect dialog. If user dismisses, cache is cleared and function returns false. App.tsx doesn't distinguish between "no cached file" vs "user dismissed reconnection". LOCATION: src/services/storage/StorageService.ts:62-88, src/App.tsx:55-60. IMPACT: Users have to manually reconnect instead of getting another chance.
- [ ] BUG-21: Missing error handling when downloadFromCloud fails during initialization - When cloudSync.downloadFromCloud() throws error during app initialization, error is caught and warning snackbar shown, but app continues with empty database. User sees "Failed to sync with cloud" but has no clear action to retry or fix. LOCATION: src/App.tsx:72-82. IMPACT: User left with empty database and no clear path forward (reload? manual sync? data lost?).
- [ ] BUG-22: Silent token acquisition failure not handled in OneDriveProvider.getAccessToken() - Method catches InteractionRequiredAuthError and shows popup, but other MSAL errors (network errors, browser issues, token refresh failures) are thrown directly without specific handling. LOCATION: src/services/storage/OneDriveProvider.ts:315-336. IMPACT: Network issues or MSAL errors during initialization result in unhelpful error messages.
- [ ] BUG-23: CloudSync.downloadFromCloud() doesn't check if provider is connected before attempting load - Method directly calls storageService.load() without verifying storage provider is properly connected. If initialization partially succeeded but provider isn't fully ready, this could fail with confusing error. LOCATION: src/services/cloudSync.service.ts:104-123. IMPACT: Could throw "No storage provider connected" at unexpected time during first login flow.
- [ ] BUG-24: OneDriveProvider.initialize() doesn't verify file still exists in cloud - When loading cached file info, provider only checks if there's cached info and if user is authenticated, but doesn't verify file still exists in OneDrive. If user deleted file from another device or OneDrive web interface, app will try to load non-existent file. LOCATION: src/services/storage/OneDriveProvider.ts:84-101. IMPACT: First load attempt fails with "file not found" error only after showing loading spinner. Poor UX.
- [ ] BUG-25: No retry mechanism when authentication popup is blocked by browser - If browser blocks authentication popup (common on iOS Safari or with strict popup blockers), error is thrown but there's no retry mechanism or helpful guidance for user. LOCATION: OneDriveProvider.ts:315-336, App.tsx:72-82. IMPACT: User sees generic error, doesn't know they need to allow popups and try again.
- [ ] BUG-26: Missing null check for baseCurrency in App.tsx initialization - checkArchivePrompt function uses baseCurrency from useBaseCurrency() hook, but this is called immediately after downloadFromCloud(). If cloud file doesn't have data yet or fails to load, baseCurrency might be undefined. LOCATION: src/App.tsx:97. IMPACT: Could cause runtime error if base currency isn't set yet.
- [x] BUG-27: 20 unused query hooks consuming bundle size - Found 20 exported query hook functions that are never used: useActiveTransactionTypes, useTransactionTypesByCategory, useTransactionTypesByGroup, useTransactionType, useAccount, useCategory, useAsset, useAssetsByType, useBudget, useBudgetsByTransactionType, useBudgetsByDateRange, useActiveBudgetsForDate, useTransaction, useTransactionsByAccount, useTransactionsByType, useTransactionsByAsset, useExchangeRate, useExchangeRatesByMonth, useLastBackupDate, useCloudProvider. LOCATION: src/hooks/queries/*.ts. IMPACT: Unnecessary code in production bundle, harder to maintain codebase, potential confusion for developers.
- [x] BUG-28: Data doesn't load to Dexie when opening existing file from Google Drive - When user opens an existing file in WelcomeDialog, setFile() is called to update currentFileItem state, then downloadFromCloud() is called immediately. However, syncService is memoized and depends on currentFileItem. State update is asynchronous, so downloadFromCloud() may execute with the old syncService that doesn't have the correct fileItem, causing the download to fail silently. ROOT CAUSE: Race condition between setState and syncService memo recalculation in SyncProvider. LOCATION: src/components/onboarding/WelcomeDialog.tsx:75, src/contexts/SyncProvider.tsx:81-85. IMPACT: User opens existing file but data doesn't appear, database stays empty. FIX: Refactored WelcomeDialog to clear Dexie and only set file, added auto-sync useEffect in SyncProvider that triggers downloadFromCloud when file is set. Changed from fullSync to downloadFromCloud to avoid write permission errors. Added snackbar error handling to show sync errors to user.
- [x] BUG-29: Adding transactions doesn't work - Dexie schema uses 'id' as primary key without auto-increment (++id), but all service create() methods try to add records without providing IDs. Dexie.add() fails silently when ID is missing. ROOT CAUSE: Schema definition in src/db/database.ts uses 'id' instead of '++id' for all tables, but no ID generation happens in service layer. LOCATION: src/db/database.ts:30-48, all *service.ts create() methods. IMPACT: Cannot add any new records (transactions, accounts, categories, etc.) - records are not saved to database. FIX: Created id.utils.ts with generateId() function using timestamp + random string. Updated all service create() methods to generate IDs before adding to database.
- [x] BUG-30: After selecting file from the start, no data is loaded until we open the settings tab - ROOT CAUSE: Initial sync effect used hasTriggeredInitialSync ref that treated all files the same - once set to true, it never triggered sync for new files. When selectFile() cleared the database and updated fileState, the syncService was recreated but the effect wouldn't run because the flag was still true from initialization. LOCATION: src/hooks/useSync.ts. IMPACT: After selecting a file, user sees empty screens until they navigate to a page that triggers a component mount or manual sync. FIX: Removed hasTriggeredInitialSync ref entirely. The effect now depends on [syncService, fullSync], which naturally triggers when file changes because syncService is memoized on [providerState, fileState]. When selectFile() updates fileState, syncService recreates, effect runs, data syncs. Also cleaned up redundant checks (isConnected, isSyncing, isInitializing) since syncService check and fullSync internal guards cover these cases.